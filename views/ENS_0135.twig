{% extends "dashboard.twig" %}

{% block content1 %}
  <div class="row">
    <div class="col-lg-3">
      <h1 class="h2">Ejericios</h1>
      <h2 >{{name}}</h2>
    </div>
    <div class="col-lg-5">

    </div>


{% endblock %}

{% block content %}



<div class="card-columns  container">
    {% for user in response %}
  <div class="card ">
    <img class="card-img-top" src="{{ user.url}}" alt="Card image cap" height="280px">
    <div class="card-body">
      <h5 class="card-title">{{user.description}}</h5>
    </div>
  </div>
   {% endfor %}
</div>



{% endblock %}
